import { loadAnnouncements, announcementImage } from "../services/announcementsService.js";
export async function renderAnnouncements(page){const root=document.querySelector("[data-announcements]");if(!root)return;try{const items=await loadAnnouncements(page);if(!items.length){root.hidden=true;return;}root.hidden=false;root.innerHTML='<div class="announcement-list">'+items.map(a=>`<article class="announcement announcement--${a.display_mode}">${a.image_path?`<img class="announcement__image" src="${announcementImage(a.image_path)}" alt="">`:""}<div><h2 class="announcement__title">${escapeHtml(a.title)}</h2><p class="announcement__description">${escapeHtml(a.short_description)}</p></div></article>`).join("")+'</div>';}catch(error){console.error("Announcements unavailable",error);root.hidden=true;}}
function escapeHtml(v){return String(v??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));}